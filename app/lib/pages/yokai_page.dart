import 'package:flutter/material.dart';

class YokaiPage extends StatelessWidget {
  const YokaiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Yokai Page"),
      ),
    );
  }
}
